# Replace empty values with NULL

Replace empty values with NULL

## Usage

``` r
NullifyEmpty(strValue)
```

## Arguments

- strValue:

  `character` The value of a field.

## Value

The value, or `NULL` if the value is "None", "All", or an empty string.
