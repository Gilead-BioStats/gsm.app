# Confirm that an object is a dfGroups

Confirm that an object is a dfGroups

## Usage

``` r
validate_dfGroups(x, dfResults)
```

## Arguments

- x:

  An object to validate.

## Value

`x`, if it passes the checks.
