# Filter to only values before a date

Filter to only values before a date

## Usage

``` r
FilterBefore(df, strField, dMaxDate)
```

## Arguments

- df:

  A data.frame to filter.

- strField:

  The name of the field.

- dMaxDate:

  The date that values should be before.

## Value

The filtered df.
