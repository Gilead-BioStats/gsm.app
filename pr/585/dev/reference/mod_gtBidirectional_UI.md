# Bidirectional gt Table Selector UI

Bidirectional gt Table Selector UI

## Usage

``` r
mod_gtBidirectional_UI(id)
```

## Arguments

- id:

  `character` The id for this element.

## Value

A [`gt::gt_output()`](https://gt.rstudio.com/reference/gt_output.html)
that handles updates from external inputs.
