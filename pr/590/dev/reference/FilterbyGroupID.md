# Filter by GroupID

Filter by GroupID

## Usage

``` r
FilterbyGroupID(df, strGroupID)
```

## Arguments

- df:

  A data.frame to filter.

- strGroupID:

  `character` A `GroupID` of an individual site or other group within a
  study.

## Value

The filtered data.frame.
