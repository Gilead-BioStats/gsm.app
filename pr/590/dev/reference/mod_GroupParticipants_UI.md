# Group Participants UI

Group Participants UI

## Usage

``` r
mod_GroupParticipants_UI(id)
```

## Arguments

- id:

  `character` The id for this element.

## Value

A [`bslib::card()`](https://rstudio.github.io/bslib/reference/card.html)
with a placeholder or a clickable table of group participants.
