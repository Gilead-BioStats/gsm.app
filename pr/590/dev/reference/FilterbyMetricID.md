# Filter by MetricID

Filter by MetricID

## Usage

``` r
FilterbyMetricID(df, strMetricID)
```

## Arguments

- df:

  A data.frame to filter.

- strMetricID:

  `character` A `MetricID` to focus on.

## Value

The filtered data.frame.
