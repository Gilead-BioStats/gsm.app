# Confirm that an object is a dfBounds

Confirm that an object is a dfBounds

## Usage

``` r
validate_dfBounds(x)
```

## Arguments

- x:

  An object to validate.

## Value

`x`, if it passes the checks.
