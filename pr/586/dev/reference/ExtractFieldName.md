# Get a field name from a variable name

Assume standard argument name format, like `strGroupID` or
`dSnapshotDate`.

## Usage

``` r
ExtractFieldName(strArgName)
```

## Arguments

- strArgName:

  The name of the argument.

## Value

`strArgName` with the beginning part removed.
