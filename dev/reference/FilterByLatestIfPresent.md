# Filter to latest snapshot if field exists

Filter to latest snapshot if field exists

## Usage

``` r
FilterByLatestIfPresent(df)
```

## Arguments

- df:

  A data.frame to filter.

## Value

The filtered df.
