# Confirm that an object is a dfMetrics

Confirm that an object is a dfMetrics

## Usage

``` r
validate_dfMetrics(x)
```

## Arguments

- x:

  An object to validate.

## Value

`x`, if it passes the checks.
