# mod_DomainCount_UI() returns the expected UI

    Code
      test_result
    Output
      Adverse Events (<span id="myDomain-count" class="shiny-html-output"></span>)

# mod_DomainCount_Server outputs the expected result

    Code
      output$count
    Output
      $html
      14
      
      $deps
      list()
      

